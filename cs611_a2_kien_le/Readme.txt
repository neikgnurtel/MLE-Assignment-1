cat > Readme.txt << 'EOF'
CS611 Assignment 2 — ML Pipeline with Airflow
Author: Le Trung Kien

GitHub repository (full source & history):
https://github.com/neikgnurtel/MLE-Assignment-2

How to run:

1. From this folder, start services:
   docker-compose up -d

2. Access Airflow Web UI:
   http://localhost:8080

3. In Airflow:
   - Enable DAG: ml_pipeline_dag
   - Trigger DAG to:
     (a) build/refresh feature store from input CSVs
     (b) train & register best model under model_store/
     (c) run inference and write predictions to datamart/gold/predictions/
     (d) run monitoring and write outputs to datamart/gold/monitoring/

Key structure:

- docker-compose.yaml       # Airflow stack definition
- Dockerfile* / Dockerfile.airflow
- dags/ml_pipeline_dag.py   # Main orchestration DAG
- utils/*.py                # ETL, feature, training, inference, monitoring logic
- data/*.csv                # Input datasets
- datamart/                 # Bronze/Silver/Gold outputs (regenerated by DAG)
- model_store/              # Best model + metadata

This ZIP is a runnable snapshot of the GitHub repository (excluding logs, local DB files, and other dev-only artifacts).
EOF